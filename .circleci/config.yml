version: 2
jobs:
  build:
    working_directory: ~/App
    docker:
      - image: beevelop/android:latest
    branches:
      ignore:
        - master
    steps:
      - run:
          name: install git
          command: apt-get update && apt-get -y install git-all
      - restore_cache:
          key: projectname-{{ .Branch }}
      - checkout
      - run:
          name: Building
          command: ./gradlew build
      - run:
          name: Testing
          command: ./gradlew test
      - save_cache:
          key: projectname-{{ .Branch }}
          paths:
            - "~/.cache"
      - store_test_results:
          path: app/build/test-results/testDebugUnitTest
      - store_test_results:
          path: app/build/test-results/testReleaseUnitTest